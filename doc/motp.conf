# Include this file in the modules section of your /etc/raddb/radiusd.conf file
# Like so:
#	$INCLUDE motp.conf

	exec motp {
		wait = yes
		# program = "/etc/raddb/otpverify.sh %{User-Name} %{User-Password} %{reply:Secret} %{reply:Pin} %{reply:Offset}"
		program = "/usr/bin/php /var/www/html/radauth.php %{User-Name} %{User-Password}"
		input_pairs = request
		output_pairs = config
	}
